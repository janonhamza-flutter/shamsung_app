import 'package:flutter/material.dart';

class AppTextStyles {
  static const splashTitle = TextStyle(
    color: Colors.white,
    fontSize: 36,
    fontWeight: FontWeight.bold,
    letterSpacing: 1.5,
  );

  static const authTitle = TextStyle(
    color: Colors.white,
    fontSize: 40,
    fontWeight: FontWeight.bold,
  );

  static const body = TextStyle(color: Colors.white, fontSize: 18);

  static const buttonText = TextStyle(
    color: Color(0xFF16C75B),
    fontSize: 18,
    fontWeight: FontWeight.bold,
  );
}
